# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.env

# Runtime state (regenerated by the sweep — machine-local, never shared)
.feed-cache.json
.sweep-seen.json

# Vault snapshot — baked from labour-second-brain at build time (npm run build)
public/vault-snapshot/

# Local Cloudflare Functions secrets (wrangler pages dev)
.dev.vars

# Wrangler local state (local KV, cache) from `wrangler pages dev`
.wrangler/

# Local env overrides (but keep .env.example tracked)
.env.*
!.env.example
